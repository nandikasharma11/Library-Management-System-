@{
    ViewData["Title"] = "Resource Not Found";
    var errorMessage = (TempData["ErrorMessage"] ?? ViewBag.ErrorMessage) as string;
}
<div class="container text-center mt-5">
    <h1 class="display-4 text-danger">Resource Not Found</h1>
    <p class="lead">
        @if (!string.IsNullOrEmpty(errorMessage))
        {
            @errorMessage
        }
        else
        {
            <text>The resource you are looking for does not exist.</text>
        }
    </p>
    <a asp-action="Index" asp-controller="Books" class="btn btn-primary">Back to Book List</a>
</div>
